# reports/figures/

Generated plots (PNG/SVG/PDF) from EDA, model training, and evaluation. Regenerate via `src/visualization/` scripts or notebooks. Most binary figure files are excluded from git (see `.gitignore`); commit only final, publication-ready figures explicitly if needed.
